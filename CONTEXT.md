# OpenInspire3 项目上下文

> 这份文档写给**协作者、后续开发者和下一个接手的会话**，回答的是
> 「为什么这样做、卡在哪、下一步做什么」。
> 面向外部用户的使用说明见 [README.md](README.md)。
>
> **接手项目时请先读完本文档再动代码。** 第五节列出的陷阱都是真实踩过的，
> 重复一次的成本是数小时。

---

## 一、这个项目在做什么

### 目标

用神经网络飞控做一台开源的「悟三」——支持硬件扩展的低成本飞行平台，
**省掉商用飞控软件的高昂成本**，为开源社区提供可复现的方案。

### 不是目标

- 不是「做一个仿真器」。仿真器是手段，用来开发和验证飞控算法。
- 不是「把 PID 调得更好」。经典控制在本项目里是**基线**与**安全网**，
  不是终点。

### 当前阶段（截至 2026-09-18）

**仿真阶段，MIL（Model-in-the-Loop）深处。** 硬件抽象层、嵌入式飞控软件、
台架测试、试飞全部未开始。

用「真实飞控开发」的阶段划分对照：

```
需求 → 建模 → 控制律 → [MIL] → SIL → PIL → HIL → 台架 → 试飞
  ○     ✅      ✅       ★      ✗     ✗     ✗     ✗      ✗
```

MIL 这一层做得比多数业余项目深（可微动力学、参数辨识、状态估计、风洞、
打杆机动、容错分析），但**阶段 2 的标准动作——频域分析（Bode / 稳定裕度）
——是跳过的**。前几轮反复撞到的「振荡」「带宽不足」「相位滞后」本质都是
频域问题，目前都在时域里用实验硬碰。

### 与目标的真实差距

| 需要 | 状态 |
|------|------|
| 仿真开发环境 | ✅ 质量不低 |
| 神经网络控制算法 | ❌ 三次尝试全部负面 |
| 飞控软件（实时/嵌入式） | ❌ 零 |
| 硬件平台 | ❌ 零 |

**项目名叫「神经网络飞控」，但目前所有能力都建立在经典控制上。**
这不是失败，但需要正视。详见第四节「关于学习型控制的判断」。

---

## 二、当前状态速查

### 能力与实测指标

| 能力 | 状态 | 关键数字 | 验证目标 |
|------|------|----------|----------|
| 六自由度动力学 | 完成 | 倾斜产生水平分量按解析解验证 | `SixDofHoverTest` |
| 可微 6-DoF | 完成 | 单步偏差 1.1e-13，200 步 3.6e-07 | `SixDofGradTest` |
| 级联 PID 定点悬停 | 完成 | 理想反馈 0.5 mm / 1.97 s | `SixDofHoverTest` |
| 状态估计 | 完成 | 姿态误差 0.32°，闭环稳态 22.3 mm | `EstimatorTest` |
| 参数辨识 | 完成 | 质量 1.9% / 阻力 14.6% / 惯量 ~18% | `ParameterIdTest` |
| 模型校准收益 | 完成 | 开环预测误差降低 11 倍 | `ModelCalibrationTest` |
| 增益由惯量推导 | 完成 | 三轴闭环特性一致，姿态残差降 12% | `AttitudeGainTest` |
| 轨迹跟踪（含前馈） | 完成 | 1 m/s 直线滞后 0.9 mm（无前馈 0.749 m） | `TrajectoryTrackingTest` |
| 风场模型 | 完成 | 常值风/阵风/Dryden 湍流 | `WindTunnelTest` |
| 抗风能力 | 完成 | 解析式吻合到 4 位小数，上限 15.65 m/s | `WindTunnelTest` |
| 手动自稳 + 打杆 | 完成 | 阶跃满舵 0.37 s，超调 −0.001° | `StickManeuverTest` |
| 混控与失效分析 | 完成 | 失效后秩 4→3，约束残差 1e-8 | `MotorFailureTest` |
| 容错旋转·姿态保持 | 完成 | 倾角 24.99°/25°，ωz 终速 10.46 rad/s | `SpinningRecoveryTest` |
| 容错旋转·位置控制 | **未开始** | 前置条件不成立，见第六节 | — |
| 学习型控制 | **未走通** | PPO 定点悬停比 PID 差 500 倍 | `TrainHoverPolicy` |

### 已知问题（诚实清单）

| 问题 | 严重度 | 位置 |
|------|--------|------|
| 偏航不可观测（无磁力计则持续漂移 60 s ≈ 16°） | 高 | `StateEstimator` |
| 容错旋转模式下悬停高度漂移约 0.9 m / 5 s | 中 | `SpinningController` |
| 位置通路不是真 EKF（结构对，增益固定） | 中 | `StateEstimator` |
| 传感器速率不匹配未建模（IMU 与控制器同为 1 kHz） | 中 | — |
| IMU 器件级效应未建模（标度因数/非正交/温度/量化） | 低 | `ImuModel` |
| 未做频域分析（稳定裕度无从查询） | 中 | — |

---

## 三、架构与数据流

### 目录职责

```
core/simulator/PhysicsEngine/   被控对象
  SixDofTypes.h       状态/配置/指令类型；SixDofCommand 定义在此（共享类型）
  SixDofDynamics      平动+转动动力学、四元数工具、RK4
  SixDofDynamicsDiff  可微版本（双实现，用于参数辨识）
  SixDofSimulator     状态机 + 执行器限幅 + 风场接入
  WindModel           常值风 / 1-cos 阵风 / Dryden 湍流
  ParameterIdentification  从轨迹辨识 质量/惯量/阻力

core/control/           控制器
  SixDofPidController     定点 PID + 轨迹跟踪（computeTracking 带前馈）
  SixDofManualController  手动自稳（杆量 → 姿态指令）
  SpinningController      三电机容错：倾角保持 + 偏航自由
  MotorMixer              控制指令 ⇄ 四电机推力（含容错重分配）
  ShootingExpert / ShootingMpc   离线规划（已验证不适用于在线）

core/estimation/        感知
  ImuModel            测量误差模型（噪声/偏置/随机游走/低通）
  StateEstimator      互补滤波姿态 + IMU 预积分位置

core/rl/                多机体素搜索（**独立旧系统，与物理引擎未打通**）
core/Map/VoxelMap       体素地图（同上）
```

### 两条状态链路（务必区分）

```
理想反馈：  真值状态 ─────────────────────────────► 控制器
真机链路：  真值 ──► 传感器(带噪) ──► 状态估计 ──► 控制器
```

**所有性能数字必须注明属于哪一种。** 0.5 mm 是前者，22.3 mm 是后者，
对外只能引用后者。

### 关键不变量

改代码时必须维持：

1. **无风/无失效时，既有实测结果逐位不变。**
   验证方式：`SixDofHoverTest` 的终止位置 `[1.0003, 1.0003, -0.9999]`、
   收敛 1.9740 s、最大倾角 22.4221°；`EstimatorTest` 的 0.0035161 / 0.022335。
2. 新增配置项默认值必须让行为退化回改造前（如 `rot_damping = 0`）。
3. 测试固定随机种子，保证可复现。

---

## 四、核心设计决策

每条格式：**决策 → 理由 → 代价**。

### D1 用六自由度而非三自由度质点模型

- **理由**：四旋翼不能直接产生水平推力，水平运动必须靠倾斜机身把竖直推力的
  一部分转向水平。这是「飞控」二字的前提，质点模型无法表达。
- **代价**：状态从 6 维到 13 维，控制必须级联，调试复杂度上升。
- **保留三自由度**：作为对照基准（同样的任务两者应给出可解释的差异）。

### D2 姿态误差的表示按场景选

- **定点控制**：用「轴角对齐机体 z 轴」。绕 z 轴的偏航不受约束（四旋翼没有
  yaw 偏好），轴角天然只约束一个自由度。
- **手动模式**：用完整四元数误差。偏航是被直接操纵的自由度，必须约束。
  且必须**取短弧**（w<0 时翻转轴），否则误差超 180° 会越纠越偏。
- **容错旋转**：回到轴角。偏航自由度已被主动放弃，用完整误差反而会把偏航
  分量经混控转嫁到 τx/τy 上，破坏倾角控制。

### D3 姿态增益由物理量推导，而非试出来

```
att_kp[i] = I[i]·ωn²        att_kd[i] = 2·ζ·I[i]·ωn
```

- **理由**：标量增益套三轴时，惯量不等会让三轴闭环特性不同（yaw 曾比 roll
  慢四成）。按带宽/阻尼比推导后三轴一致，且惯量变化时增益自动跟随。
- **前提**：惯量要准。手填值与真值差 33~50% → 增益误差 28~44%。
  所以 D4。

### D4 参数靠辨识而非手填

- **理由**：惯量直接决定增益与机动特性，手填误差折算成增益误差 28~44%。
  可微仿真能从实测轨迹辨识（质量 1.9% / 阻力 14.6% / 惯量 ~18%），
  换算成开环预测精度是**误差降低 11 倍**。
- **代价**：需要带激励的轨迹数据。可辨识性由数据决定，不由算法决定
  （无激励时阻力误差 58%）。

### D5 控制律必须有参考前馈

- **背景**：定点控制律 `a = kp·(p_ref−p) + kd·(0−v)` 隐含假设期望速度恒为零。
- **实测**：1 m/s 直线跟踪滞后恒为 `(kd/kp)·v = 0.749 m`（与预测差 0.2%）；
  圆周相位滞后满足 `sinφ = (kd/kp)·ω`。
- **决策**：`computeTracking` 加 `a_ref` 与 `v_ref` 前馈。直线滞后降到 0.9 mm。
- **代价**：需要轨迹的二阶信息（位置/速度/加速度）。

### D6 混控必须支持失效重分配

- **背景**：原实现用 4×4 逆矩阵算四个电机推力再丢弃失效者 —— 分配完全错误
  （三个电机各 2.45 N，总推力 7.36 N 而需要 9.81 N）。
- **决策**：失效后改用 `T`、`τx`、`τy` 三行配三个健康电机求解。
  该 3×3 子矩阵行列式为 `4a² ≠ 0`，**恰好可逆**。
- **代价**：放弃 τz 的控制权 → 偏航自由旋转。这是容错旋转的物理起点。
- **保留 `redistribute=false`**：用于模拟「飞控没有失效检测逻辑」的对照。

### D7 容错旋转的选择

- **背景**：四旋翼失去一个电机后，混控矩阵 4×4 → 4×3，秩降为 3。
  实测约束 `τz = c·T − (c/a)·τx + (c/a)·τy`，即 **T/τx/τy 可控，只有 τz
  失去自由**。
- **决策**：主动放弃偏航，让飞行器绕偏航轴自由旋转，同时用 τx/τy 把机身
  倾住。倾角在机体系中固定 → 推力方向在惯性系中……**（注意：这里有个坑，
  见第五节 T4）**。
- **代价**：不能悬停；倾角受零力矩分配限制，上限 `cosθ ≥ mg/(2·f_max)`。

### D8 关于学习型控制的判断（重要）

三条路径均得负面结论：

| 路线 | 结论 |
|------|------|
| 离线专家生成（打靶法） | 时间尺度不匹配：120 段解约 200 s，PPO 需要上千条 |
| 在线 MPC | 单次重规划 0.96 s，实时性差两个数量级 |
| 参数辨识 | **唯一正面落点**（离线、一次性、不要求实时） |

定点悬停上 PPO 比 PID 差 500 倍；搜索任务上 MAPPO 目标发现率 0/20
（在刷探索奖励），而贪心基线 20/20。

**判断**：共同原因是**所选任务太简单**——简单方法的先验正好匹配问题结构，
学习需要海量样本才能重新发现同样的先验。

**因此下一步不建议继续挑战定点悬停**，而应选经典方法确实做不好的任务，
并考虑**混合架构**（经典控制器保证稳定性，神经网络只做前馈补偿与扰动估计）。
风洞已提供可控扰动源，是这条路的起点。

---

## 五、调试陷阱（真实踩过，按类型整理）

### T1 坐标系与符号（**最高频，占已犯错误的三分之一**）

- **NED 的 z 轴朝下。** 机体 z 轴与之同向：水平时 `z_cur = [0,0,+1]`，
  倾斜 θ 后第三分量为 **`+cosθ`**。写成 `−cosθ` 会让目标与水平姿态的夹角
  变成 `acos(−cosθ)`——θ=25° 时是 **155° 而非 25°**，控制器从第一帧起就在
  纠正一个不存在的巨大误差。**这一个符号是「从精确目标倾角出发也保不住」
  的全部原因。**
- **姿态误差要求两个量在同一坐标系中比较。** 把目标写在机体系
  （`[sinθ,0,cosθ]`）再转到 NED，而当前机体 z 轴在机体系中恒为 `[0,0,1]`
  ——两者夹角永远等于 θ，控制器会持续输出力矩把飞行器推翻。
  重力定义在 NED，基准只能选 NED。
- **倾斜方位不能由控制律指定。** 用「当前偏航角」定义目标方向会形成正反馈：
  倾斜带动偏航变 → 目标跟着转 → 控制器追旋转目标 → 翻滚加剧（ωx 冲到
  52 rad/s）。正确做法是只约束倾角**大小**，方位由角动量决定。
- **风的方向约定**：`WindModel` 用「吹向」而非气象学的「来向」，差 180°。

### T2 执行机构的顺序

- **混控必须先分配、后限幅、再失效**，不能「先按满编求解再丢弃失效者」。
- **逐电机限幅**：电机只能正转且有上限，限幅在混控层而非控制器层
  （控制器无从知道分配结果是否超限）。
- **推力补偿必须用实际姿态**，不能用目标姿态。手动模式第一版用目标倾角补偿，
  打满舵时**爬升 2.933 m**（不是掉高）——过渡期实际倾角小于指令，
  按目标补偿等于持续过推，而爬升速度建立后不会自行消失。改用实际姿态后
  高度偏离降到 0.004 m。

### T3 有效推力上限不总是「电机数 × 单机上限」

维持**零力矩**（`τx = τy = 0`）时三电机方程有唯一解 `f2 = f4`、**`f3 = 0`**
——只有对角两个电机出力。所以倾角上限是 `cosθ ≥ mg/(2·f_max)`，
不是 `mg/(3·f_max)`。按后者算会把上限高估一倍多（49° vs 实际 11.2°）。

### T4 绕机体 z 轴旋转**不等于**绕竖直轴旋转

- 容错旋转需要**推力方向在 NED 中扫圈**才能做周期调制。
- 推力沿机体 −z，方向即机体 z 轴。**绕机体 z 轴旋转不改变机体 z 轴的方向**
  ——调制的正负半周互相抵消，净水平力为零。
- 需要绕**竖直轴**旋转，机体系中对应 `ω = Ω·[−sinθ, 0, cosθ]`，
  **必须含 x 分量**。而 τz 不可控使角速度自然沿机体 z 轴增长（实测 ω_x ≈ 0）。
- 附带约束：维持该旋转需抵消 `τ_gyro,x = Ω²·sinθ·cosθ·I_z`，
  配合 `torque_limit` 得 `Ω ≤ 9.66 rad/s`。

### T5 角动量会锁死姿态

偏航自旋到 37 rad/s 时角动量 `I·ω ≈ 1.04 kg·m²/s`，飞行器像陀螺一样被锁住，
控制力矩上限 1 N·m 根本掰不动，表现为倾角卡在 138° 不再收敛。
**所以 `SixDofConfig::rot_damping` 是必需的**（气动阻尼让自旋趋于终速
`τz/k`，也就给容错留下一个时间窗）。

### T6 解析预测差固定倍数时，先怀疑公式

- 角加速度实测 5.66 对预测 7.73（差 27%）——不是模型错了，是解析式漏了
  阻尼项。正确式 `α = (τz − k·ω)/Izz`，用一阶系统解析解在采样区间两端取值
  相减后吻合到 3.8%。
- 反过来，直线跟踪实测 0.7490 对预测 0.7500（差 0.13%）说明模型完整。
- **偏差量级本身携带信息**：精确吻合 = 模型完整；差十几二十个百分点 = 漏了一项；
  差几倍 = 理解错了。

### T7 加新机制前，先确认现有机制被正确实现

「从目标倾角出发也保不住」时，我判断陀螺耦合是原因，加了 `ω×(Iω)` 前馈
——**结果一点没变**（92° vs 93°）。真正根因是一个符号。
**若加一个物理上正确的补偿项而效果完全不变，说明扰动不是主导项**，
应立刻转向而不是再试一轮。

### T8 序列化风场时，物理与前馈必须看到同一个风

把风速改成「预生成序列」以支持预知实验时，容易只让控制器侧用序列而忘了给
仿真器 `setWind` —— 结果是物理世界无风、前馈却在补偿一个不存在的扰动，
被动组误差为零、前馈组 0.4958 m，两组数字都自洽但都错。

**序列化的前提是物理与控制器看到同一个风**，这一条必须显式保证。

### T9 对照实验前先检查基线本身是否有建模误差

风前馈第一版写成 `a_ff = −k·|v_wind|·v_wind / m`，即把 `v_rel` 简化成
`−v_wind`（假设飞行器静止）。稳态等价，但湍流下飞行器始终在动，该式高估阻力。

**用带误差的基线做对照，会让后续「学习是否有空间」的结论失去意义** ——
网络赢的可能只是「修正了基线的错」，而非学到了新东西。所以：
先让解析基线达到它理论上的最优形式，再谈学习能带来什么。

### T10 用理论上必须为零的量做判据

最有效的一次诊断：失效后完全不给姿态控制，只给恒定推力，测出 `τx/τy = 5e-17`
（精确为零）。这一步把「混控错了」与「控制器错了」彻底分开，
排查范围从「完全失控」压缩到「姿态控制律」。
**零是最强的判据——没有任何参数能凑出零来。**

---

## 六、开发约定

### 写测试

1. **先写解析预测，再跑仿真。** 预测与实测的对照是定位问题的唯一高效手段。
   无法给出解析预测时，至少给出对照实验（同条件 A/B）。
2. **断言必须量被测对象主导的量。** 反例：用闭环 MPC 性能评价模型准确度
   （MPC 受视野限制）；用位置收敛时间评价姿态增益（位置环没变）。
3. **阈值要有依据。** 从噪声模型、物理约束推导，不凭感觉。
   反例：曾写「偏置估计残差 < 真值的 10%」，而噪声底已是真值的 26%，
   该判据物理上不可能满足。
4. **未解决的问题改为「记录」而非删除断言。** 让已知失败的实验长期红着会
   掩盖真正通过的条目；删掉则丢失追溯依据。折中：诊断段有断言，
   主实验段输出过程量并标注「已知问题」。
5. **固定随机种子。**
6. **每一条断言都要能说清「它在验证什么物理事实」。**

### 改代码

1. **改控制律时保持既有回归逐位不变。** 加轨迹跟踪入口时，定点路径的浮点
   运算序列被刻意保持原样（写成 `kp*e + kd*(-vel)` 而非 `kd*(0-vel)`）。
   验证：`SixDofHoverTest` + `EstimatorTest` 逐位一致。
2. **新配置项默认值必须退化回改造前行为。** `rot_damping` 默认 0、
   `drag_coeff` 默认 0、`redistribute` 默认 true 但无失效时行为不变。
3. **共享类型放类型头文件。** `SixDofCommand` 曾在 `SixDofPidController.h`
   里，迫使其他控制器反向包含它。
4. **提交信息写清「为什么」与「实测数据」，不写「做了什么」。**
   本项目的历史提交里有大量决策依据，是重要的知识资产。

### 判断何时停止

- **前置条件检查**：动手前先问「我的方案依赖什么前提，当前满足吗」。
  周期调制做了两轮才发现「依赖推力方向扫圈，而当前旋转轴不会扫圈」——
  这是纯损失。若一开始问这一句，五分钟就能发现。
- **迹象识别**：如果连续两轮调整参数而结果形状不变，说明问题不在参数，
  应停下来做定位实验。

---

## 七、路线图与待决事项

### 已完成

- [x] 六自由度动力学与闭环控制
- [x] 可微动力学与参数辨识
- [x] 状态估计与传感器模型
- [x] 轨迹跟踪与参考前馈
- [x] 风场模型与抗风测试
- [x] 手动模式与打杆机动
- [x] 混控失效分析与容错重分配
- [x] 容错旋转·姿态保持

### 待决（需要先做判断，不是直接开工）

**1. 容错旋转·位置控制**

需要先回答：**绕竖直轴的旋转状态是否可达且可维持？**
做法：用真值直接构造该状态，验证角动量守恒能否维持住。
若不可达，后续全部不用做。这是 T7 教训的直接应用。

若可达，需要一个**角速度控制器**（不是姿态控制器）：既要维持倾角 θ，
又要把角速度矢量控制到 `Ω·[−sinθ, 0, cosθ]`。难点在于 `ω_z` 只能经约束
`τz = c·T − (c/a)·τx + (c/a)·τy` 间接影响。

**2. 混合架构：经典控制器 + 神经网络补偿**

判断：**性价比高于继续推容错旋转**。已具备的条件——
风洞提供可控扰动源、经典基线摆在那里做对照、每一步都能验证。
网络只学「经典控制器做不好的那一小块」，而非端到端输出电机指令。

**但「那一小块」已被量化收窄（见 `WindFeedforwardTest`）**，不能笼统说「学抗风」：

| 子任务 | 收益上界 | 判断 |
|--------|---------|------|
| 补偿常值风 | 解析式已达 1e-4 量级 | ❌ 无学习空间 |
| 预测未来扰动 | 预知 0.2 s 仅改善 5.3% | ❌ 不值得投入 |
| **补偿估计延迟** | 延迟 0.2 s 可改善 15.3% | ✅ **目标应定在此** |

关键区分：**「预测未来」与「推断当前」不是一回事**。前者受扰动可预测性限制
（Dryden 湍流时间常数 2 s，0.2 s 预知收益很小）；后者受延迟幅度限制，而延迟
本身能把阵风场景的误差翻倍（0.0968 → 0.1925）。

因此学习目标应定为「从历史观测推断**当前**风速」以抵消估计延迟，
而不是「预测未来风速」。

**3. 频域分析**

补上 Bode / 增益裕度 / 相位裕度。前几轮遇到的振荡与带宽问题本质都是频域
问题，目前全靠时域实验硬碰。成本低（依赖已有模型），且能给将来的神经网络
控制器提供闭环频响对照。

### 长期（明确未开始）

- 硬件抽象层、嵌入式飞控软件、实时系统
- 台架测试（执行器标定、振动、电磁兼容）
- HIL 测试
- 试飞

### 明确不建议

- 继续在定点悬停上训练学习型控制器（D8）。
- 在标准四旋翼上寻找「单电机失效后维持全姿态可控」的方法——
  数学上不可能（`MotorFailureTest` 已证明）。

---

## 八、常见任务指南

### 加一个新的控制器

1. 若它产出 `SixDofCommand`，无需派生 `SixDofController`——那个接口的语义是
   「给定目标位置」，姿态类/杆量类控制器套用会别扭（`SixDofManualController`
   就是独立类）。
2. 增益按 D3 由惯量推导。
3. 写测试时先给解析预测。

### 加一个新的测试

1. 参照 `TrajectoryTrackingTest` / `WindTunnelTest` 的结构：
   打印表格 + 解析预测列 + 对照列 + 断言。
2. 在 `CMakeLists.txt` 里加 `add_executable` + `target_link_libraries(... oi3_simulator)`。
3. 快回归集（每次改动后跑）：
   `SixDofHoverTest StickManeuverTest WindTunnelTest TrajectoryTrackingTest EstimatorTest MotorFailureTest`
4. 慢测试（按需）：`MpcHoverTest`、`ParameterIdTest`、`ModelCalibrationTest`。

### 加一个传感器或风场模型

- 传感器：在 `core/estimation/ImuModel` 旁新增，接口返回物理量而非张量
  （估计器在 1 kHz 热路径上，标量实现更快也更好核对）。
- 风场：派生 `WindModel`，在 `SixDofSimulator::setWind` 接入。
  注意 `at(t)` 按**时间**推进状态而非按调用次数（RK4 会在同一时刻多次查询）。

### 排查「姿态不对」类问题

按 T1 的顺序做**一次性审计**，不要逐步试错：
1. 列出所有涉及的坐标系（机体 / NED）与各量的定义域；
2. 确认 NED 的 z 轴方向，检查所有第三分量的符号；
3. 用单位姿态打印 `z_cur`、`z_des`，确认夹角等于预期值；
4. 确认误差的构造在两个量在同一坐标系中比较。

---

## 附：相关文档

| 文档 | 内容 |
|------|------|
| [README.md](README.md) | 对外说明：能力、构建、运行 |
| 本文档 | 对内说明：设计决策、陷阱、约定、路线图 |
| 代码注释 | 逐处的「为什么这样写」，重要决策都注在定义处 |
